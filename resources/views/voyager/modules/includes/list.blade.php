<h3><i class="voyager-terminal"></i> {{ __('voyager::compass.commands.title') }} <small>{{ __('voyager::compass.commands.text') }}</small></h3>

En cours de construction
{{-- <h4>Modules Actifs</h4>
<ul>
    @foreach($modules_enable as $module_enable)
   <li>{{ $module_enable->getName() }}</li>
    @endforeach
</ul>

<h4>Modules Inactifs</h4>
<ul>
    @foreach($modules_disable as $module_disable)
   <li>{{ $module_disable->getName() }}</li>
    @endforeach
</ul> --}}
